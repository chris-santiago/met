defaults:
  - default

eval:
  cls:
    _target_: sklearn.linear_model.LogisticRegression
    max_iter: 3000
  train_data:
    _target_: met.data.get_covertype_dataset
    train: True
  test_data:
    _target_: met.data.get_covertype_dataset
    train: False